@page "/profile/{culture?}/{username?}"

@model Server.Pages.Features.Identity.ProfileModel

@{
	var fullName =
		$"{Model.ViewModel.FirstName} {Model.ViewModel.LastName}".Trim();

	ViewData[Constants.ViewDataKeyName.PageTitle] = fullName;
}

<section-form>
	<fieldset>
		<section-form-header>
			@(fullName)
		</section-form-header>

		<partial name="PartialViews/_DisplayPageMessages" />

		<dtat-full-input asp-for="ViewModel.CellPhoneNumber" dir="ltr" read-only=true />

		<dtat-full-input asp-for="ViewModel.EmailAddress" dir="ltr" read-only=true />

		<dtat-full-input asp-for="ViewModel.Hits" dir="ltr" read-only=true />

		<hr />

		@(Html.Raw(Model.ViewModel.Description))
	</fieldset>
</section-form>

